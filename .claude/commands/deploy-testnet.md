---
description: Deploy a HSK testnet (133) y verificación en Blockscout
---
Usa el subagente `contract-deployer` para desplegar en HSK testnet (133) con `forge script`, verificar en Blockscout, guardar direcciones en `deployments/133.json` y actualizar `SESSION.md`. Antes, confirma que `/test` y `/audit` estén en verde. No uses mainnet.
