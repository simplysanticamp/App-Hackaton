---
description: Corre tests de contratos y build del front
---
Ejecuta `forge fmt --check`, `forge test -vvv` y el build del front (`npm run build` si existe). Resume fallos con archivo:línea y propón fix; no cambies nada sin decirlo.
